
class Deck(object):
    def __init__(self, name, cards):
      """ Creates a new Deck
          @param name The name of the deck
          @param cards A list of cards in the deck
      """
      self.name = name
      self.cards = cards
